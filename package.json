{
  "name": "tasco",
  "version": "0.4.3",
  "description": "A modern task runner, with support for monorepo and caching",
  "files": [
    "./dist"
  ],
  "bin": "./dist/cli.js",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsup src/cli.ts --format esm --target node16 --no-shims",
    "test": "vitest run"
  },
  "dependencies": {
    "execa": "^6.0.0",
    "fast-glob": "^3.2.7"
  },
  "devDependencies": {
    "@types/cross-spawn": "^6.0.2",
    "@types/js-yaml": "^4.0.5",
    "colorette": "^2.0.16",
    "esbuild": "^0.14.2",
    "esbuild-register": "^3.2.1",
    "glob-regex": "^0.3.2",
    "joycon": "^3.1.1",
    "js-yaml": "^4.1.0",
    "strip-ansi": "^7.0.1",
    "tinyargs": "^0.1.4",
    "tsup": "^5.11.9",
    "typescript": "^4.5.4",
    "vite": "^2.7.4",
    "vitest": "^0.0.118"
  }
}
